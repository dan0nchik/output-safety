<source>
  @type kafka_group
  brokers kafka:9092
  consumer_group fluentd-kafka-graylog
  topics final-results
  format json
  add_headers true
</source>

<match final-results>
  @type gelf
  host graylog
  port 12201
  flush_interval 2s
</match>
